{
  "config": {
    "abort": {
      "single_instance_allowed": "Only one instance is allowed."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "General settings",
        "description": "Configure the global behavior of ESPHome OTA campaigns.",
        "data": {
          "timeout": "Failure timeout (seconds)",
          "max_items": "Maximum devices per batch",
          "throttle": "Enable automatic delay adaptation"
        }
      },
      "throttle": {
        "title": "Automatic delay adaptation",
        "description": "Choose which sensors should be used to estimate server load, then define the minimum and maximum delay between updates.",
        "data": {
          "cpu_sensor": "CPU usage sensor (%)",
          "temp_sensor": "CPU temperature sensor (°C)",
          "load_sensor": "System load sensor",
          "delay_min": "Minimum delay between updates (s)",
          "delay_max": "Maximum delay between updates (s)"
        }
      }
    }
  }
}
